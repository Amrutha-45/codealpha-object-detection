# =========================================================
# Backend Requirements — AI Object Detection & Tracking API
# =========================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- AI / Computer Vision ---
ultralytics==8.2.28          # YOLOv8 (detection) + built-in ByteTrack support
opencv-python-headless==4.10.0.82
numpy==1.26.4
torch==2.3.1
torchvision==0.18.1

# --- Tracking ---
lapx>=0.5.5                   # required by ByteTrack association step (Windows pre-built wheel)
filterpy==1.4.5               # Kalman filter utils

# --- Data Validation / Config ---
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# --- Utilities ---
aiofiles==23.2.1
loguru==0.7.2
Pillow==10.3.0

# --- CORS / Middleware (bundled with FastAPI, listed for clarity) ---
starlette==0.37.2
